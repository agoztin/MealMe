<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:colorBackground">@color/colorBackground</item>
    </style>

    <!-- Fragment Main -->
    <style name="FragmentMain" parent="AppTheme">
        <item name="android:textColor">@color/defaultTextColor</item>
    </style>

    <style name="FragmentMain.ResultTitle">
        <item name="android:textSize">28sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textAlignment">center</item>
    </style>

    <style name="FragmentMain.ResultItem">
        <item name="android:textSize">25sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:shadowColor">#000000</item>
        <item name="android:shadowDx">1.5</item>
        <item name="android:shadowDy">1.5</item>
        <item name="android:shadowRadius">1.5</item>
    </style>

    <!-- Fragment Detail -->
    <style name="FragmentDetail" parent="AppTheme">
        <item name="android:textColor">@color/fmainInputTextSearch</item>
    </style>

    <style name="FragmentDetail.Title">
        <item name="android:textColor">@color/fdetailTitle</item>
        <item name="android:textSize">40sp</item>
        <item name="android:textAlignment">center</item>
    </style>

    <style name="FragmentDetail.Ingredients">
        <item name="android:textColor">@color/fdetailIngredients</item>
        <item name="android:textSize">16sp</item>
    </style>

    <style name="FragmentDetail.Instructions">
        <item name="android:textColor">@color/fdetailInstructions</item>
        <item name="android:textSize">16sp</item>
    </style>

</resources>
